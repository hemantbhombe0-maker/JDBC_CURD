# JDBC_CURD
JDBC CRUD refers to performing Create, Read, Update, and Delete operations on a database using Java’s JDBC API. It allows Java applications to interact with relational databases, managing data efficiently and enabling dynamic, platform-independent data handling.
